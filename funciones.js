//formas de escribir una funcion: 

//1:

function suma(x) {
    return x + 3;
}
//2 : funcion in to the variable

var suma = function (x) {
    return x + 3; 
};

//3: function flecha ( remplaza la palabra function por una flecha)

var suma = (x) => {
    return x + 3
};